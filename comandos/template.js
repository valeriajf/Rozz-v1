const { prefix, botName, donoName, DarkApikey, DarkUrl, emoji } = require("../dono/config.json")

module.exports = {
  nome: "NOME",
  descricao: "DESCRIÇÃO",
  comandos: ["COMANDO", "COMANDO2"],
  uso: `${prefix}COMANDL`,

  handle: async ({ enviar, q, selo, client, from, data, hora}) => {
//CÓDIGO 
  },
};